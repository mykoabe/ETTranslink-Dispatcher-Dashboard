const { Typography } = require("@mui/material");
import AppRequestLoad from "src/components/requested-load/requested-load";

const AppLoadRequest = () => {
  return <AppRequestLoad />;
};
export default AppLoadRequest;
